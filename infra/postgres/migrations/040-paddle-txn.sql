-- Последняя сделка, открытая клиентом в окне оплаты.
--
-- Нужна ровно для одного: узнать подписку, не дожидаясь вебхука. Пока
-- вебхука нет, идентификатора подписки у нас тоже нет, и спросить
-- Paddle не о чем — кроме как об этой сделке, которую мы сами и
-- завели. По ней он назовёт и клиента, и подписку.
ALTER TABLE tenants ADD COLUMN IF NOT EXISTS paddle_txn text;
